# Stuvi

## Workflow

`git pull`

In your homestead VM (homestead ssh):

```
composer install
php artisan migrate
php artisan db:seed
mkdir config/administrator/settings
```

## Administrator

url: `/admin`
